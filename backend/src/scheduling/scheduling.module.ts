import { Module } from '@nestjs/common';

/**
 * SchedulingModule — implementa `proponer_turnos` y `agendar_turno` (usa
 * `slot_is_available`; deja los turnos en estado `proposed`).
 * (Esqueleto: sin implementación todavía.)
 */
@Module({})
export class SchedulingModule {}
