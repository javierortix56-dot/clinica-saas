import { Module } from '@nestjs/common';

/**
 * AiModule — cliente LLM (abstracción intercambiable Gemini/Anthropic/OpenAI),
 * loop de function calling y definiciones de las 8 herramientas.
 * (Esqueleto: sin implementación todavía.)
 */
@Module({})
export class AiModule {}
