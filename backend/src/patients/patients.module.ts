import { Module } from '@nestjs/common';

/**
 * PatientsModule — funciones de lectura de pacientes (`buscar_paciente_por_dni`,
 * etc.).
 * (Esqueleto: sin implementación todavía.)
 */
@Module({})
export class PatientsModule {}
