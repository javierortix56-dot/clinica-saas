import { Module } from '@nestjs/common';

/**
 * ConversationModule — gestiona `conversations` / `conversation_messages` y
 * enruta `phone_number_id → clinic_id`.
 * (Esqueleto: sin implementación todavía.)
 */
@Module({})
export class ConversationModule {}
