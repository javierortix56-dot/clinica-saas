NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_URL=http://localhost:3000

# Resumen clínico con IA (Server Action — la key NO se expone al cliente).
# Misma key de Google AI Studio que usa el backend (prefijo 'AIza').
# Si queda vacía, el botón "Generar resumen" devuelve un error claro.
GEMINI_API_KEY=
