// TODO: listado de pacientes.
// admin / reception: todos los pacientes de la clínica (RLS filtra por clinic_id del JWT).
// professional: solo sus propios pacientes (RLS filtra además por professional_id).
export default function PatientsPage() {
  return <div>Pacientes</div>;
}
